export default function Loading() {
  return (
    <main id="main" className="mx-auto w-full max-w-lg px-4 pt-10">
      <p className="text-xs font-semibold uppercase tracking-widest text-accent">Shotgun.Rocks 🤘</p>
      <h1 className="mt-2 text-2xl font-bold">Opening your invitation…</h1>
      <p className="mt-2 text-sm text-muted">Waking things up — this takes a few seconds the first time.</p>
    </main>
  );
}
